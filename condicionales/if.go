package main

import (
	"fmt"
)

func main() {
	if 5 == 10 {
		fmt.Println("true")
	} else if 5 == 5 {
		fmt.Println("else if")
	} else {
		fmt.Println("False")
	}

	// --------------------------------------------------------------------
	// ------------------------------- Notas ------------------------------
	// --------------------------------------------------------------------
	// paréntesis opcionales
	// puede ir == != < > <= >= && || !
	// la llave debe ir en la misma linea del if
}
